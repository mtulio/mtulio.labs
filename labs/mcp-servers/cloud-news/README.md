# MCP Server experiment | Cloud News

Build:
```sh
go build mcp_server_news_aws.go
```

Try:

```sh
curl http://localhost:8080/news
```

Try the remote version in the Vercenl platform:

```sh
curl https://labs-git-api-mcp-news-aws-marco-bragas-projects.vercel.app/api/news/aws
```
