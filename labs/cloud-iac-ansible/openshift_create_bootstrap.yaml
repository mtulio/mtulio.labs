---
- include: openshift_create_bootstrap_setup.yaml

- name: OpenShift Bootstrap Setup
  hosts: localhost
  connection: local

  vars_files:
    - "vars/compute/ocp-bootstrap-aws.yaml"

  roles:
    - role: mtulio.cloud-compute

- include: openshift_config_dump.yaml
