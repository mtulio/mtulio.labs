---

#AWS: https://docs.ansible.com/ansible/latest/collections/community/aws/route53_module.html
cloud_dns_zones:
  # public
  - zone: "{{ cluster_state.zones.base }}"
    openshift_type: base
    provider: aws

    # records:
    #   - record: "api.{{ ocp_install_infraID }}.{{ ocp_config_baseDomain }}"
    #     type: A
    #     value: "{{ openshift_lb_public_dns }}"
    #     alias: yes

  # private
  - zone: "{{ cluster_state.zones.cluster }}"
    openshift_type: cluster
    provider: aws
    vpc_name: ocp-vpc-use1
    vpc_region: us-east-1

    # records:
    #   - record: "api.{{ ocp_install_infraID }}.{{ ocp_config_baseDomain }}"
    #     type: A
    #     value: "{{ openshift_lb_public_dns }}"
    #     alias: yes
    #   - record: "api-int.{{ ocp_install_infraID }}.{{ ocp_config_baseDomain }}"
    #     type: A
    #     value: "{{ openshift_lb_private_dns }}"
    #     alias: yes
