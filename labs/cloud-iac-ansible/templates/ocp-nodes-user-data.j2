{
  "ignition": {
    "config": {
      "merge": [
        {
          "source": "{{ openshift_userdata.config_source }}"
        }
      ]
    },
    "security": {
      "tls": {
        "certificateAuthorities": [
          {
            "source": "{{ openshift_userdata.ca_source }}"
          }
        ]
      }
    },
    "version": "3.2.0"
  }
}
