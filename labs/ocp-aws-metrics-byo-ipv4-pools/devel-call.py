from handler import lambda_handler

# Define the event variable
event = {}

# Define the context variable
context = {}

# Call the lambda_handler function with the required arguments
lambda_handler(event, context)
