# OpenShift day-0 | Install etcd in an isolated node

Idea / Goal:

- Describe the steps to hack the manifests / ignition files to install a etcd in sepparated node from control plane


Problems to consider:

- Lattency
